declare namespace UserType {
  export type payload = {
    name: string
    lastName: string
    email: string
    password: string
    confirmPassword: string
  }

  export type item = {
    id: number
    name: string
    lastName: string
    email: string
    password: string
  }
}
